  version: '3.8'

  services:
    web:
      context: .
      dockerfile: Dockerfile.flask
      ports:
        - "5000:5000"
      environment:
        - FLASK_ENV=production
      volumes:
        - .:/app
